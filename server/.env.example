PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/gminsta
JWT_SECRET=supersecretjwtkey_gminsta_2026
JWT_EXPIRE=30d

# Cloudinary (Optional - Fallback to local files if empty)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# SMTP Email Configuration (Optional - Fallback to terminal logs if empty)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@gminsta.com
FROM_NAME=GMinsta
